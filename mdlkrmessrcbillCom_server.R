# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

# krmes
# mdlkrmessrcbillServer::krmesstocksrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesroutesrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesproductionwarehousinsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesproductionreturnsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesproductionrequisitionsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesproductionordersrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesproductionfeedsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesprocesstransfersrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesprocessreportsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

mdlkrmessrcbillServer::krmesprocessplanningsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesprocesssrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesmaterialsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesflexvaluessrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesempsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesdeptsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)

# mdlkrmessrcbillServer::krmesbomsrcbillServer(input = input,output = output,session = session,dms_token = dms_token)



  

